import { styled } from '@mui/material';

export const CheckoutContent = styled('form')(({ theme }) => ({
  padding: theme.spacing(7),
  backgroundColor: theme.palette.background.default,
  borderRadius: theme.shape.borderRadius,

}));
