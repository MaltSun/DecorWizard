import {  Button, styled } from "@mui/material";

export const SwitchButton = styled(Button)(() => ({
    width: '40px',
    border: '2px solid',
    fontWeight: 'bold',
    fontSize: '27px',
    color: '#FD8E53',
 }));